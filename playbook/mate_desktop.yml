---
- hosts: all
  tags: [x11, MATE]
  tasks:
    - name: Ensure MATE desktop environment is installed
      become: yes
      apt:
        name: mate-desktop-environment
        state: present
    - name: Ensure customizations available
      become: yes
      apt:
        pkg:
          - shiki-brave-theme
          - fonts-firacode
          # - blueman
